You are a senior Python backend and database engineer.

I have a Flask SaaS platform currently using SQLite with Flask-SQLAlchemy.
I want to migrate the project to PostgreSQL safely without breaking the application.

IMPORTANT RULES:
- Do NOT change any routes
- Do NOT rename models
- Do NOT modify business logic
- Only update database configuration and migration tools
- The application must continue working exactly the same

Tech stack:
Flask
Flask-SQLAlchemy
Blueprint architecture
Gunicorn
Nginx
Ubuntu server

Tasks:

1. Detect current database configuration

Find the current database URI, which should be something like:

SQLALCHEMY_DATABASE_URI = "sqlite:///database.db"

2. Replace SQLite with PostgreSQL

Update configuration to use environment variables:

import os

SQLALCHEMY_DATABASE_URI = os.getenv(
    "DATABASE_URL",
    "postgresql://finora:password@localhost/finora_db"
)

Ensure SQLAlchemy works with PostgreSQL.

3. Add PostgreSQL driver

Add to requirements.txt:

psycopg2-binary

If missing, also ensure:

Flask-SQLAlchemy
Flask-Migrate

4. Add Flask-Migrate

Install migration system.

Update app initialization:

from flask_migrate import Migrate

db = SQLAlchemy(app)
migrate = Migrate(app, db)

5. Create migration folder

Prepare commands:

flask db init
flask db migrate
flask db upgrade

6. Ensure models compatibility

Check models for SQLite-only syntax such as:

AUTOINCREMENT
sqlite-specific types

Replace with SQLAlchemy compatible types.

Example:

id = db.Column(db.Integer, primary_key=True)

7. Add PostgreSQL install instructions

Provide commands for Ubuntu VPS:

sudo apt update
sudo apt install postgresql postgresql-contrib

Create database and user:

sudo -u postgres psql

CREATE USER finora WITH PASSWORD 'password';
CREATE DATABASE finora_db;
GRANT ALL PRIVILEGES ON DATABASE finora_db TO finora;

8. Add .env configuration

DATABASE_URL=postgresql://finora:password@localhost/finora_db

9. Add database health check

Ensure the app verifies DB connection on startup.

10. Provide a migration script for existing SQLite data.

Steps:

- read SQLite database
- export tables
- insert data into PostgreSQL

Use SQLAlchemy session to migrate data.

11. Ensure the app runs with:

gunicorn wsgi:app

12. Output final project structure and confirm migration readiness.

The application must remain fully functional.