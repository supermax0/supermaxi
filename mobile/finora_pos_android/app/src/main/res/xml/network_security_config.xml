<?xml version="1.0" encoding="utf-8"?>
<!-- يسمح بـ HTTP على الشبكة المحلية (تجارب التطوير). الإنتاج يُفضّل HTTPS فقط. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
