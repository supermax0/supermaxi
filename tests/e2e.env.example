PLAYWRIGHT_BASE_URL=http://127.0.0.1:5008
# الافتراضي في helpers.js: supermax / admin / dev12345 (أو ضبط PLAYWRIGHT_PASSWORD)
# فتح كل بنود القائمة محلياً: py scripts/dev_full_access.py supermax
# PLAYWRIGHT_TENANT_SLUG=supermax
# PLAYWRIGHT_USERNAME=admin
# PLAYWRIGHT_PASSWORD=dev12345
# تشغيل بدون واجهة (CI): PW_HEADLESS=1
