# Feature flags

Defaults seeded in `permissions.DEFAULT_FEATURE_FLAGS` / `schema_guard`.

Toggle from `/mobile-app/flags` or admin API. Bootstrap returns current flags + branding + maintenance to the app.
