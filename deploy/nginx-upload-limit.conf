# إصلاح خطأ 413 عند رفع ملفات كبيرة (فيديو/صورة في النشر التلقائي)
# أضف السطر التالي داخل بلوك server { } في إعدادات موقعك (مثلاً /etc/nginx/sites-available/finora):

    client_max_body_size 110M;

# مثال كامل:
# server {
#     server_name finora.company;
#     client_max_body_size 110M;   # <-- أضف هذا السطر
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
#
# بعد التعديل:
#   sudo nginx -t
#   sudo systemctl reload nginx
