apiVersion: v1
kind: Service
metadata:
  name: ai-automation-backend
spec:
  selector:
    app: ai-automation-backend
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
